

#include <stdio.h>

int main() {
    printf("                    Hello, world!\r2Hello, world!");
    /* apprently '/r' replaces the text from the beginning of the string with
    the text that follows*/
    return 0;
}